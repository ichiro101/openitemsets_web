
<!-- ITEM SET LISTING AREA -->
<div class="row">
  <div class="col-lg-10">
    <%= bootstrap_form_tag :url => item_sets_path, :method => :get, :layout => :inline do |f| %>
      <%= f.collection_select(:champion, Champion.all, :id, :display_name, {:selected => params[:champion], :include_blank => "No Champion Filters"}) %>
      <%= f.submit "Filter" %>
    <% end %>

    <% @item_sets.each do |item_set| %>
      <%= render :partial => "partials/item_set_media", :locals => {:item_set => item_set} %>
    <% end %>
  <%= paginate @item_sets %>
  </div><!-- col -->
  <div class="col-md-2">
    <h4>Actions</h4>
    <ul class="nav nav-pills nav-stacked">
      <li><%= link_to("Create Item Set", new_item_set_path) %></li>
    </ul>
  </div><!-- col -->
</div><!-- row -->
